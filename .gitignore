# OMC (oh-my-claudecode) 로컬 상태 — 커밋 대상 아님
.omc/

# ESP-IDF 빌드 산출물
build/
sdkconfig.old
# 생성되는 개발자별 설정(Finnhub 키·WiFi 비밀번호 포함) — 절대 커밋 금지. 공유 설정은 sdkconfig.defaults
sdkconfig
*.pyc
__pycache__/

# ESP-IDF 컴포넌트 매니저가 받은 의존성 (재생성 가능)
managed_components/
dependencies.lock

# LVGL 시뮬레이터 스크린샷 산출물
sim/shots/

# clangd / editor index caches
.cache/

# macOS
.DS_Store

# host test/sim run logs
*.log
